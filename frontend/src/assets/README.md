# Frontend Assets

This folder contains static assets used by the React application.

- `logo.svg` — Application logo
- `hero-illustration.svg` — Home page hero illustration
- `empty-state.svg` — History page empty state illustration
- `agent-flow.svg` — Agent pipeline diagram

Import assets in components:

```jsx
import logo from '../assets/logo.svg';
```

